{
  "name": "playwrightproject",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:unauth:firefox": "cross-env BROWSER=firefox PW_PROJECT=unauthenticated npx playwright test --project=unauthenticated --reporter=line,allure-playwright",
    "test:auth:firefox": "cross-env BROWSER=firefox PW_PROJECT=authenticated npx playwright test --project=authenticated --reporter=line,allure-playwright",
    "test:unauth:chrome": "cross-env PLAYWRIGHT_HTML_REPORT=playwright-report/unauth BROWSER=chromium PW_PROJECT=unauthenticated npx playwright test --project=unauthenticated --reporter=html",
    "test:auth:chrome": "cross-env PLAYWRIGHT_HTML_REPORT=playwright-report/auth BROWSER=chromium PW_PROJECT=authenticated npx playwright test --project=authenticated --reporter=html ",
    "allure:generate": "allure generate ./allure-results --clean",
    "allure:open": "allure open ./allure-report",
    "test:dynamic": "cross-env PW_PROJECT=$npm_config_project npx playwright test --project=%npm_config_project% --reporter=line,allure-playwright && npm run allure:generate && npm run allure:open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.9.1",
    "allure-playwright": "^3.4.2",
    "cross-env": "^10.1.0"
  }
}